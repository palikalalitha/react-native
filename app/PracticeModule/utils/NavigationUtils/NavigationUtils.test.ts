// Implement NavigationUtils using tdd

// import React from 'react';
// import {render, fireEvent} from 'react-native-testing-library';

describe('Sample test case to get started', () => {
  it('sample test case to get started', () => {
    const text = 'ADD';
    expect(text).toBeDefined();
  });
});
